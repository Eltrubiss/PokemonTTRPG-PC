import path from "path";

export const DATA_PATH = "data";

export const GENERATED_PATH = path.join(
    DATA_PATH,
    "generated"
);

export const MANUAL_PATH = path.join(
    DATA_PATH,
    "manual"
);

export const SAVES_PATH = path.join(
    DATA_PATH,
    "saves"
);